import React from 'react';

const UserList = ({ users }) => {
  return (
    <div className="user-list">
      <h2>Liste des utilisateurs</h2>
      {/* Affichez les utilisateurs ici */}
    </div>
  );
};

export default UserList;
